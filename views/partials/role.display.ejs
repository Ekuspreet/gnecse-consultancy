<% if (role === 'student') { %>
    Student
<% } else if (role === 'tcc') { %>
    Testing & Consultancy Cell
<% } else if (role === 'mentor') { %>
    Mentor
<% } else if (role === 'admin') { %>
    Administrator
<% } else if (role === 'alumni') { %>
    Alumni
<% } %>
